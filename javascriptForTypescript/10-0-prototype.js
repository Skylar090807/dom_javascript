{
  // Prototype
  /*
    TypeScript는 JavaScript의 Superset이다.
    - class
    - interface
    - generics
    - types
    등을 이용해서 객체지향 프로그래밍을 할 수 있다.

    JavaScript도 엄밀이 따지면 객체지향 프로그램이다.
    Prototype based이기 때문이다. JavaScript도 
    ES6 부터 class-like를 사용할 수 있는데, class-like도 Proto-based이다.
    
    typeScript에서 작업한 interface와 class도 compiler를 이용해 JavaScript로 변환하면
    결국 전부 Prototype이 된다.

    JavaScript에서 Prototype은 근본적이라고 할 수 있다.
  */
  /*
      Prototype?
      Prototype for Inheritance.
      Prototype은 상속을 위해 쓰인다.
      typeScript에서 class를 구현하고 extends를 이용해서 상속하는 것처럼
      JavaScript에서는 Prototype을 통해 상속을 구현할 수 있다.
      Prototype 속성과 함수들을 정의.
    */
  /*
        Prototype-based Programming
        - a style of OOP 객체지향 프로그래밍의 한 방식
        - behavior reuse(inheritance) 행동들을 재사용 가능(상속)
        - by reusing existing objects 기존 존재하는 오브젝트를 재사용 가능
        - that serve as prototype 
      */
}
